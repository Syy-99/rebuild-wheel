# Lab: mmap

实现`mmap`系统调用，将文件映射到内存上

### 实验步骤

1. 首先需要在用户地址空间中找到一块空闲的区域，用于映射mmap页

    - 考虑用户地址空间结构，为了避免冲突，我们可以设置mmap映射的区域从`trapframe`开始，向下分配

2. 根据实验报告书提示，定义 vma 结构体，并且在 proc 结构体末尾为每个进程加上 16 个 vma 空槽。

3. 实现`mmap`系统调用

4. 实现`munmap`系统调用